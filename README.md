# 固收公众号日报 · Gitea 静态发布测试

这是一个零依赖静态网页测试项目，只包含 HTML、CSS 和 JavaScript。

## 本地预览

直接打开 `index.html`，或在本目录运行任意静态文件服务器。

## 发布内容

- `index.html`：页面结构
- `styles.css`：页面样式及响应式布局
- `app.js`：示例数据、日期、分类、搜索和文章预览交互

仓库本身只保存文件。若公司 Gitea 未配置 Pages，需要由管理员提供一个静态站点地址，并将这三个文件发布到对应目录。
